'use strict';

var fs = require('fs');

var _require = require('./get-build-tools-installer-path'),
    getBuildToolsInstallerPath = _require.getBuildToolsInstallerPath;

var _require2 = require('./get-python-installer-path'),
    getPythonInstallerPath = _require2.getPythonInstallerPath;

/**
 * Cleans existing log files
 */


function cleanExistingLogFiles() {
  var files = [getBuildToolsInstallerPath().logPath, getPythonInstallerPath().logPath];

  files.forEach(function (file) {
    if (fs.existsSync(file)) {
      fs.unlinkSync(file);
    }
  });
}

module.exports = { cleanExistingLogFiles: cleanExistingLogFiles };